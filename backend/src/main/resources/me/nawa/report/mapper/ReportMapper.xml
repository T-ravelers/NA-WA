<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.nawa.report.mapper.ReportMapper">

    <resultMap id="reportResultMap"
               type="me.nawa.report.domain.Report"
               autoMapping="true">
        <result property="reportContent"
                column="report_content"
                typeHandler="me.nawa.explore.mapper.JsonNodeTypeHandler"/>
    </resultMap>

    <select id="findJourneyForUpdate"
            resultType="me.nawa.report.domain.ReportJourney">
        SELECT
            trip_id,
            member_id,
            title,
            start_date,
            end_date
        FROM trips
        WHERE trip_id = #{tripId}
          AND deleted_at IS NULL
        FOR UPDATE
    </select>

    <select id="findJourneyById"
            resultType="me.nawa.report.domain.ReportJourney">
        SELECT
            trip_id,
            member_id,
            title,
            start_date,
            end_date
        FROM trips
        WHERE trip_id = #{tripId}
          AND deleted_at IS NULL
    </select>

    <select id="findActiveReportByTripId"
            resultType="me.nawa.report.domain.Report">
        SELECT
            report_id,
            trip_id,
            generation_status,
            locale
        FROM reports
        WHERE trip_id = #{tripId}
          AND deleted_at IS NULL
        ORDER BY report_id DESC
        LIMIT 1
    </select>

    <select id="findTimelineItemsByTripId"
            resultType="me.nawa.report.domain.ReportTimelineItem">
        SELECT
            ti.trip_item_id,
            ti.item_id,
            ti.visit_date,
            ei.item_type,
            COALESCE(e.title, p.name) AS title,
            ti.trip_item_status AS status
        FROM trip_items ti
        JOIN explore_items ei
          ON ei.item_id = ti.item_id
         AND ei.deleted_at IS NULL
        LEFT JOIN event e
          ON e.event_id = ei.item_id
         AND ei.item_type = 'EVENT'
         AND e.deleted_at IS NULL
        LEFT JOIN place p
          ON p.place_id = ei.item_id
         AND ei.item_type = 'PLACE'
         AND p.deleted_at IS NULL
        WHERE ti.trip_id = #{tripId}
          AND ti.deleted_at IS NULL
          AND (
              (ei.item_type = 'EVENT' AND e.event_id IS NOT NULL)
              OR (ei.item_type = 'PLACE' AND p.place_id IS NOT NULL)
          )
        ORDER BY ti.visit_date ASC,
                 ti.display_order ASC,
                 ti.trip_item_id ASC
    </select>

    <!--
      정산으로 회수한 금액. settlements.source_transfer_id가 NOT NULL이자 UNIQUE라
      결제 하나에 정산이 최대 하나 붙는다. FROM을 건드리지 않는 상관 서브쿼리로 두어
      findEligibleExpensesForUpdate의 FOR UPDATE가 정산 행까지 잠그지 않게 한다.

      회수액은 request_status='PAID'인 몫만 센다. 원결제자 본인 행은 만들 때
      NOT_REQUESTED로 들어가고 PENDING에서만 PAID로 넘어가므로(EqualSettlementCreator·
      ItemizedSettlementCreator·SettlementPaymentServiceImpl), 이 합은 남에게서 실제로
      받은 돈과 같다. 아직 받지 못한 몫은 빼지 않는다 — 리포트는 스냅샷이라 나중에
      받아도 갱신되지 않고, 못 받은 돈을 미리 빼면 실제로 부담한 액수보다 작게 남는다(#543).
    -->
    <sql id="settledAmount">
        (SELECT COALESCE(SUM(sm.share_amount), 0)
         FROM settlements s
         JOIN settlement_members sm
           ON sm.settlement_id = s.settlement_id
          AND sm.request_status = 'PAID'
          AND sm.deleted_at IS NULL
         WHERE s.source_transfer_id = t.transfer_id
           AND s.deleted_at IS NULL)
    </sql>

    <!--
      정산 지급 이체는 실제 지급일이 아니라 원 결제의 날짜와 카테고리에 귀속한다.
      리포트는 여정 종료 뒤에 만들 수 있고 정산도 주로 그 뒤에 지급되므로, 지급일로
      기간을 자르면 결제자에게서 뺀 금액이 지급자의 지출에는 더해지지 않아 사라진다.
      settlement_members.paid_transfer_id가 UNIQUE라 각 지급 이체의 원 결제는 최대 하나다.
    -->
    <sql id="settlementSourceCompletedAt">
        (SELECT source_t.completed_at
         FROM settlement_members sm
         JOIN settlements s
           ON s.settlement_id = sm.settlement_id
          AND s.deleted_at IS NULL
         JOIN wallet_transfers source_t
           ON source_t.transfer_id = s.source_transfer_id
          AND source_t.deleted_at IS NULL
         WHERE sm.paid_transfer_id = t.transfer_id
           AND sm.request_status = 'PAID'
           AND sm.deleted_at IS NULL)
    </sql>

    <sql id="settlementSourceCategory">
        (SELECT source_t.spending_category
         FROM settlement_members sm
         JOIN settlements s
           ON s.settlement_id = sm.settlement_id
          AND s.deleted_at IS NULL
         JOIN wallet_transfers source_t
           ON source_t.transfer_id = s.source_transfer_id
          AND source_t.deleted_at IS NULL
         WHERE sm.paid_transfer_id = t.transfer_id
           AND sm.request_status = 'PAID'
           AND sm.deleted_at IS NULL)
    </sql>

    <sql id="expenseOccurredOn">
        DATE(COALESCE(<include refid="settlementSourceCompletedAt" />, t.completed_at))
    </sql>

    <sql id="expenseCategory">
        COALESCE(NULLIF(<include refid="settlementSourceCategory" />, ''),
                 NULLIF(t.spending_category, ''), 'OTHER')
    </sql>

    <!--
      지출은 결제액이 아니라 내가 실제로 부담한 순액이다. 정산 이체(SETTLEMENT)로
      들어온 CREDIT 원장을 음수로 더하지 않고 원 결제 행에서 빼는 이유는 세 가지다.
      정산금은 여정이 끝난 뒤 들어와 아래 기간 조건 밖이고, 정산 이체에는
      spending_category가 없어 카테고리 구성이 음수 조각을 갖게 되며,
      trip_expense_links가 ledger_entry_id에 UNIQUE라 참가자 수만큼 연결이 늘어난다.
      원 결제 행에서 빼면 날짜와 카테고리가 원 결제 그대로 남는다.

      참가자가 낸 정산 몫은 그 사람의 DEBIT으로 잡되, 원 결제의 날짜와 카테고리를
      쓴다. 정산은 결제 하나당 하나뿐이므로 뺀 값이 음수가 될 수 없다.
    -->
    <sql id="eligibleExpenseColumns">
        t.transfer_id,
        le.ledger_entry_id,
        le.amount - <include refid="settledAmount" /> AS amount,
        <include refid="expenseOccurredOn" /> AS occurred_on,
        <include refid="expenseCategory" /> AS category,
        t.memo
    </sql>

    <select id="findExpenseCandidates"
            resultType="me.nawa.report.domain.ReportExpense">
        SELECT <include refid="eligibleExpenseColumns" />,
               CASE WHEN tel.trip_id = #{tripId} THEN TRUE ELSE FALSE END AS selected
        FROM wallet_ledger_entries le
        JOIN wallet_transfers t ON t.transfer_id = le.transfer_id
        JOIN wallets w ON w.wallet_id = le.wallet_id AND w.deleted_at IS NULL
        JOIN wallet_owners wo ON wo.wallet_owner_id = w.wallet_owner_id
            AND wo.member_id = #{memberId} AND wo.deleted_at IS NULL
        JOIN trips tr ON tr.trip_id = #{tripId} AND tr.member_id = #{memberId}
            AND tr.deleted_at IS NULL
        LEFT JOIN trip_expense_links tel ON tel.ledger_entry_id = le.ledger_entry_id
        WHERE le.entry_type = 'DEBIT' AND le.deleted_at IS NULL
          AND t.initiator_member_id = #{memberId} AND t.deleted_at IS NULL
          AND t.currency_code = 'KRW' AND t.transfer_status = 'COMPLETED'
          AND t.transfer_type IN ('QR_PAYMENT', 'SETTLEMENT')
          AND <include refid="expenseOccurredOn" /> BETWEEN tr.start_date AND tr.end_date
          AND (
              tel.ledger_entry_id IS NULL
              OR (tel.trip_id = #{tripId} AND tel.deleted_at IS NULL)
          )
        ORDER BY t.completed_at ASC, t.transfer_id ASC
    </select>

    <select id="findEligibleExpensesForUpdate"
            resultType="me.nawa.report.domain.ReportExpense">
        SELECT <include refid="eligibleExpenseColumns" />,
               FALSE AS selected
        FROM wallet_ledger_entries le
        JOIN wallet_transfers t ON t.transfer_id = le.transfer_id
        JOIN wallets w ON w.wallet_id = le.wallet_id AND w.deleted_at IS NULL
        JOIN wallet_owners wo ON wo.wallet_owner_id = w.wallet_owner_id
            AND wo.member_id = #{memberId} AND wo.deleted_at IS NULL
        JOIN trips tr ON tr.trip_id = #{tripId} AND tr.member_id = #{memberId}
            AND tr.deleted_at IS NULL
        WHERE le.entry_type = 'DEBIT' AND le.deleted_at IS NULL
          AND t.initiator_member_id = #{memberId} AND t.deleted_at IS NULL
          AND t.currency_code = 'KRW' AND t.transfer_status = 'COMPLETED'
          AND t.transfer_type IN ('QR_PAYMENT', 'SETTLEMENT')
          AND <include refid="expenseOccurredOn" /> BETWEEN tr.start_date AND tr.end_date
          AND t.transfer_id IN
          <foreach collection="transferIds" item="transferId" open="(" separator="," close=")">
              #{transferId}
          </foreach>
        FOR UPDATE
    </select>

    <select id="findLinkedLedgerEntryIdsByTripId" resultType="long">
        SELECT ledger_entry_id FROM trip_expense_links
        WHERE trip_id = #{tripId} AND deleted_at IS NULL
        ORDER BY ledger_entry_id ASC
    </select>

    <select id="findLinkedTripIdByLedgerEntryId" resultType="long">
        SELECT trip_id FROM trip_expense_links
        WHERE ledger_entry_id = #{ledgerEntryId}
    </select>

    <insert id="insertTripExpenseLink">
        INSERT INTO trip_expense_links (trip_id, ledger_entry_id)
        VALUES (#{tripId}, #{ledgerEntryId})
    </insert>

    <insert id="insertReport" useGeneratedKeys="true" keyProperty="reportId">
        INSERT INTO reports
            (trip_id, generation_status, locale, report_content, generated_at)
        VALUES
            (#{tripId}, #{generationStatus}, #{locale},
             #{reportContent,
               typeHandler=me.nawa.explore.mapper.JsonNodeTypeHandler},
             CURRENT_TIMESTAMP)
    </insert>

    <select id="findReportById"
            resultMap="reportResultMap">
        SELECT
            r.report_id,
            r.trip_id,
            t.member_id,
            t.title,
            t.start_date,
            t.end_date,
            r.generation_status,
            r.locale,
            r.report_content,
            r.generated_at,
            r.created_at
        FROM reports r
        JOIN trips t
          ON t.trip_id = r.trip_id
         AND t.deleted_at IS NULL
        WHERE r.report_id = #{reportId}
          AND r.deleted_at IS NULL
    </select>

    <select id="findReportsByMemberId"
            resultType="me.nawa.report.domain.Report">
        SELECT
            r.report_id,
            r.trip_id,
            t.member_id,
            t.title,
            t.start_date,
            t.end_date,
            r.generation_status,
            r.locale,
            r.generated_at,
            r.created_at
        FROM reports r
        JOIN trips t
          ON t.trip_id = r.trip_id
         AND t.deleted_at IS NULL
        WHERE t.member_id = #{memberId}
          AND r.deleted_at IS NULL
        ORDER BY r.created_at DESC,
                 r.report_id DESC
    </select>

    <!-- ── 비교(#398) ───────────────────────────────────────────────────── -->

    <select id="findComparisonMember"
            resultType="me.nawa.report.domain.ReportComparisonMember">
        SELECT
            member_id,
            display_name,
            profile_image_url,
            nationality_code
        FROM members
        WHERE member_id = #{memberId}
          AND deleted_at IS NULL
    </select>

    <!--
      같은 약속 동료. 내 약속은 두 갈래 — 리포트 여정의 trip_items(CONFIRMED)가 가리키는 약속과,
      appointment_members.trip_id가 이 여정인 내 ACTIVE 참여. #407부터 만들 때도 참여할 때도 둘 다
      채워지므로 방장·참가자가 같은 길로 잡힌다. 활동일로 근사하던 갈래는 #415에서 뺐다 — 기간이
      겹치는 다른 여정의 동료가 섞였다. 호스트도 appointment_members 행을 가지므로 따로 더하지 않는다.
    -->
    <select id="findComparisonPeerMembers"
            resultType="me.nawa.report.domain.ReportComparisonMember">
        SELECT DISTINCT
            m.member_id,
            m.display_name,
            m.profile_image_url,
            m.nationality_code
        FROM (
            SELECT ti.appointment_id
            FROM trip_items ti
            WHERE ti.trip_id = #{tripId}
              AND ti.trip_item_status = 'CONFIRMED'
              AND ti.appointment_id IS NOT NULL
              AND ti.deleted_at IS NULL
            UNION
            SELECT am.appointment_id
            FROM appointment_members am
            WHERE am.member_id = #{memberId}
              AND am.trip_id = #{tripId}
              AND am.membership_status = 'ACTIVE'
              AND am.deleted_at IS NULL
        ) mine
        JOIN appointments a ON a.appointment_id = mine.appointment_id
            AND a.deleted_at IS NULL
            AND a.appointment_status != 'CANCELLED'
        JOIN appointment_members am2 ON am2.appointment_id = a.appointment_id
            AND am2.member_id != #{memberId}
            AND am2.membership_status = 'ACTIVE'
            AND am2.deleted_at IS NULL
        JOIN members m ON m.member_id = am2.member_id
            AND m.deleted_at IS NULL
        ORDER BY m.member_id ASC
    </select>

    <!--
      비교용 지출 정의. findEligibleExpensesForUpdate와 같은 필터(결제자 본인 지갑의 DEBIT·
      KRW·COMPLETED·QR/정산·여정 기간)에서 trip_expense_links 조건만 뺐다 — 동료는 내 여정에
      지출을 연결할 수 없으므로 연결 여부로 거르면 동료 쪽이 항상 0이 된다. 나도 같은 정의로
      합산한다. 금액도 같은 정의라 settledAmount로 회수액을 뺀다 — 여기서만 빼지 않으면
      같은 사람의 상세 화면 총액과 비교 화면 총액이 갈린다. 종료 뒤 지급된 정산도 빠지지
      않도록 정산 지급 이체의 날짜와 카테고리는 원 결제에서 가져온다.
    -->
    <sql id="comparisonExpenseSource">
        FROM wallet_ledger_entries le
        JOIN wallet_transfers t ON t.transfer_id = le.transfer_id
        JOIN wallets w ON w.wallet_id = le.wallet_id AND w.deleted_at IS NULL
        JOIN wallet_owners wo ON wo.wallet_owner_id = w.wallet_owner_id
            AND wo.member_id = t.initiator_member_id AND wo.deleted_at IS NULL
        WHERE le.entry_type = 'DEBIT' AND le.deleted_at IS NULL
          AND t.deleted_at IS NULL
          AND t.currency_code = 'KRW' AND t.transfer_status = 'COMPLETED'
          AND t.transfer_type IN ('QR_PAYMENT', 'SETTLEMENT')
          AND <include refid="expenseOccurredOn" /> BETWEEN #{startDate} AND #{endDate}
    </sql>

    <select id="findComparisonSpending"
            resultType="me.nawa.report.domain.ReportComparisonSpending">
        SELECT
            t.initiator_member_id AS member_id,
            <include refid="expenseCategory" /> AS category,
            SUM(le.amount - <include refid="settledAmount" />) AS amount
        <include refid="comparisonExpenseSource" />
          AND t.initiator_member_id IN
          <foreach collection="memberIds" item="id" open="(" separator="," close=")">
              #{id}
          </foreach>
        GROUP BY t.initiator_member_id, <include refid="expenseCategory" />
        ORDER BY t.initiator_member_id ASC, amount DESC, category ASC
    </select>

    <resultMap id="cohortSnapshotResultMap"
               type="me.nawa.report.domain.ReportCohortSnapshot"
               autoMapping="true">
        <result property="analytics"
                column="analytics"
                typeHandler="me.nawa.explore.mapper.JsonNodeTypeHandler"/>
    </resultMap>

    <!-- 같은 국적 회원의 최신 COMPLETED 리포트 하나씩. analytics가 없는 옛 스냅샷은 null로 온다. -->
    <select id="findSimilarCohortAnalytics"
            resultMap="cohortSnapshotResultMap">
        SELECT
            ranked.member_id,
            ranked.analytics
        FROM (
            SELECT
                t.member_id,
                JSON_EXTRACT(r.report_content, '$.analytics') AS analytics,
                ROW_NUMBER() OVER (
                    PARTITION BY t.member_id
                    ORDER BY r.created_at DESC, r.report_id DESC
                ) AS rn
            FROM reports r
            JOIN trips t ON t.trip_id = r.trip_id AND t.deleted_at IS NULL
            JOIN members m ON m.member_id = t.member_id AND m.deleted_at IS NULL
            WHERE r.generation_status = 'COMPLETED'
              AND r.deleted_at IS NULL
              AND m.nationality_code = #{nationalityCode}
              AND m.member_id != #{memberId}
              AND m.account_type = 'TRAVELER'
        ) ranked
        WHERE ranked.rn = 1
        ORDER BY ranked.member_id ASC
        LIMIT #{limit}
    </select>

</mapper>
