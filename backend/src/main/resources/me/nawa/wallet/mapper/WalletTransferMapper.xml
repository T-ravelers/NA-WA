<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.nawa.wallet.mapper.WalletTransferMapper">
    <sql id="walletTransferColumns">
        transfer_id,
        initiator_member_id,
        transfer_number,
        transfer_type,
        transfer_status,
        amount,
        memo,
        spending_category,
        spending_type,
        completed_at,
        created_at,
        idempotency_key
    </sql>

    <select id="findByTransferId" resultType="me.nawa.wallet.domain.WalletTransfer">
        SELECT
        <include refid="walletTransferColumns"/>
        FROM wallet_transfers
        WHERE transfer_id = #{transferId}
          AND deleted_at IS NULL
    </select>

    <select id="findByIdempotencyKey" resultType="me.nawa.wallet.domain.WalletTransfer">
        SELECT
        <include refid="walletTransferColumns"/>
        FROM wallet_transfers
        WHERE idempotency_key = #{idempotencyKey}
          AND deleted_at IS NULL
            LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="transferId">
        INSERT INTO wallet_transfers
            (currency_code, initiator_member_id, transfer_number, transfer_type, transfer_status,
             amount, memo, spending_category, spending_type, completed_at, idempotency_key)
        VALUES
            ('KRW', #{initiatorMemberId}, #{transferNumber}, #{transferType}, #{transferStatus},
             #{amount}, #{memo}, #{spendingCategory}, #{spendingType}, #{completedAt}, #{idempotencyKey})
    </insert>
</mapper>
