<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.nawa.wallet.mapper.WalletMapper">

    <select id="findByMemberId" resultType="me.nawa.wallet.domain.Wallet">
        SELECT
            w.wallet_id,
            w.currency_code,
            w.available_balance,
            w.wallet_status
        FROM wallets w
        JOIN wallet_owners o ON o.wallet_owner_id = w.wallet_owner_id
        WHERE o.member_id = #{memberId}
            AND o.deleted_at IS NULL
            AND w.deleted_at IS NULL
    </select>

    <insert id="insertMemberWalletOwner"
            useGeneratedKeys="true"
            keyProperty="walletOwnerId"
            keyColumn="wallet_owner_id">
        INSERT INTO wallet_owners (
            member_id,
            owner_type
        ) VALUES (
            #{memberId},
            'MEMBER'
        )
    </insert>

    <!-- available_balance(0), wallet_status(ACTIVE), version(0)은 스키마 기본값을 그대로 쓴다 -->
    <insert id="insertMemberWallet"
            useGeneratedKeys="true"
            keyProperty="walletId"
            keyColumn="wallet_id">
        INSERT INTO wallets (
            wallet_owner_id,
            currency_code
        ) VALUES (
            #{walletOwnerId},
            #{currencyCode}
        )
    </insert>

    <select id="findByWalletIdForUpdate" resultType="me.nawa.wallet.domain.Wallet">
        SELECT wallet_id, currency_code, available_balance, wallet_status
        FROM wallets
        WHERE wallet_id = #{walletId} AND deleted_at IS NULL
        FOR UPDATE
    </select>

    <update id="updateBalance">
        UPDATE wallets
        SET available_balance = #{availableBalance}
        WHERE wallet_id = #{walletId}
    </update>
</mapper>
