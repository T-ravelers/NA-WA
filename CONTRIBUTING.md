# NA-WA 공통 협업 가이드

이 문서는 NA-WA의 Issue, 브랜치, 커밋, PR과 문서 작성 규칙을 정의합니다. 작업을
시작하기 전에 전체 흐름을 확인하고, 구현할 영역의 개발 컨벤션을 함께 따르세요.

- [프론트엔드 개발 컨벤션](./frontend/docs/DEVELOPMENT_CONVENTION.md)
- [백엔드 개발 컨벤션](./backend/docs/DEVELOPMENT_CONVENTION.md)

## 1. 작업 시작

> [!IMPORTANT]
> 모든 작업은 Issue를 만든 뒤 시작합니다.

1. 작업 목적과 완료 조건을 Issue에 작성합니다.
2. `New issue`에서 작업 성격에 맞는 양식을 선택합니다.
3. 한 Issue에는 하나의 주요 목적만 둡니다.
4. 최신 `main`에서 Issue 번호가 포함된 작업 브랜치를 만듭니다.
5. API, 환경 변수, 배포 또는 공통 계약을 변경하면 관련 문서도 같은 PR에서
   수정합니다.

기본 브랜치는 `main`입니다. `develop` 브랜치는 사용하지 않습니다.

작업 흐름은 `Issue → 작업 브랜치 → main 대상 PR → 리뷰 → Squash and merge`입니다.

## 2. Issue 작성

제목은 `[Type] 한글 설명` 형식을 사용합니다.

```text
[Feat] 여행 일정 등록 기능 구현
[Fix] 로그인 실패 예외 응답 수정
[Docs] 프론트엔드 개발 규칙 정리
```

Issue 양식에서 다음 내용을 구체적으로 작성하세요.

- 작업 목적
- 구현 범위와 제외 범위
- 영향 영역
- 검증 방법
- 완료 조건
- 관련 문서, API 명세, 화면 또는 참고 링크

다른 팀원이 Issue만 읽어도 작업 범위와 완료 여부를 판단할 수 있어야 합니다.

## 3. 브랜치 만들기

브랜치 이름은 `<type>/#<issue-number>-<short-description>` 형식을 사용합니다. 설명은
영문 kebab-case로 짧게 작성하세요.

| Type       | 용도                                |
| ---------- | ----------------------------------- |
| `feature`  | 기능 추가                           |
| `fix`      | 버그 수정                           |
| `refactor` | 동작을 유지하는 구조 개선           |
| `chore`    | 설정, 의존성, 빌드 또는 인프라 작업 |
| `test`     | 테스트 추가 또는 수정               |
| `docs`     | 문서만 변경                         |

```text
feature/#12-create-travel-plan
fix/#27-login-error-response
docs/#31-frontend-convention
```

`main`에 직접 push하지 마세요. 모든 변경은 PR을 통해 병합합니다.

## 4. 커밋 작성

커밋 메시지는 `<type>: <한글 요약>` 형식을 사용합니다. 변경 영역을 구분해야 한다면
`<type>(<scope>): <한글 요약>`을 사용하세요. 관련 Issue 번호는 메시지 끝에 추가할
수 있습니다.

```text
feat(frontend): 여행 일정 등록 폼 추가 (#12)
fix(backend): 로그인 실패 예외 응답 수정 (#27)
docs: 프론트엔드 개발 규칙 정리 (#31)
```

- 하나의 커밋에는 하나의 논리적인 변경만 담습니다.
- 독립적으로 설명하거나 되돌릴 수 있는 API 연동, UI, 테스트와 문서 변경은
  커밋을 나눕니다.
- 의미 없는 중간 메시지와 포맷 변경만 섞인 커밋을 남기지 않습니다.
- 민감정보, 빌드 산출물과 개인 IDE 설정을 커밋하지 않습니다.

## 5. PR 작성

PR 제목은 Issue와 같은 `[Type] 한글 설명` 형식을 사용합니다. 조직 공통 PR 양식의
모든 항목을 확인하세요.

- 변경 목적과 주요 변경
- 실행 안내와 관련 Issue
- 영향 영역과 검증 결과
- 계약과 운영 영향
- 화면 자료
- 알려진 제한과 위험
- 체크리스트, 리뷰 안내와 후속 작업

기본 브랜치를 대상으로 하는 PR에는 `Closes #<issue-number>`를 사용합니다. 기본
브랜치가 아닌 통합 브랜치를 대상으로 한다면 `Refs #<issue-number>`를 사용하고,
최종 `main` 대상 PR에서 Issue를 종료하세요.

리뷰가 필요한 결정과 알려진 제약을 숨기지 마세요. 프론트엔드, 백엔드와 인프라
영향을 구분하고 검증 결과를 구체적으로 작성하세요.

## 6. 변경 사항 검증

변경한 영역에 해당하는 검증을 모두 수행하세요.

### Frontend

```shell
pnpm format:check
pnpm lint
pnpm type-check
pnpm --filter @na-wa/frontend test:unit --run
pnpm build
```

사용자 흐름을 변경했다면 관련 Playwright 테스트도 실행하세요.

```shell
pnpm test:e2e
```

### Backend

```shell
cd backend
./gradlew build --no-daemon
```

API를 변경했다면 단위·통합 테스트 또는 Swagger/Postman 확인 결과를 PR에
작성하세요.

문서만 변경하더라도 링크, 명령, 코드 예시, Markdown 형식과 실제 구현의 일치 여부를
확인하세요.

## 7. 리뷰하고 병합하기

- PR을 열기 전에 변경 파일과 비밀정보 포함 여부를 확인합니다.
- 리뷰 피드백을 반영하거나 반영하지 않는 이유를 답변으로 남깁니다.
- 필수 검증이 실패한 상태로 병합하지 않습니다.
- 코드 리뷰와 승인을 받기 전에 작성자가 직접 병합하지 않습니다.
- `Squash and merge`로 PR의 커밋을 하나의 이력으로 정리합니다.
- squash commit 제목은 PR 전체 변경을 설명하도록 작성합니다.
- 병합 후 원격 작업 브랜치를 삭제합니다.
- 관련 없는 프론트엔드와 백엔드 변경을 하나의 PR에 섞지 않습니다.
- 공통 API 계약이나 배포 변경처럼 함께 검토할 이유가 있을 때만 여러 영역을 같은
  PR에서 다룹니다.

## 8. 문서 작성하기

새 문서를 만들거나 기존 문서를 수정할 때
[토스 테크니컬 라이팅 가이드](https://technical-writing.dev/)를 참고하세요. 토스
가이드의 원칙을 그대로 복사하기보다 NA-WA의 독자, 코드와 작업 흐름에 맞게
적용합니다.

### 문서 구조

- 제목 바로 아래에서 대상 독자와 문서를 읽고 얻을 결과를 설명합니다.
- 한 문서는 하나의 주요 목적에 집중합니다.
- 독자가 가장 먼저 필요한 정보를 앞에 배치합니다.
- 제목만 읽어도 내용을 예측할 수 있도록 구체적인 동사와 대상을 사용합니다.
- 상세 설명을 반복하지 않고 기준 문서로 연결합니다.

### 문장과 용어

- 한 문장에는 하나의 생각만 담습니다.
- 작업 지침은 주체, 행동, 경로, 명령과 조건을 구체적으로 작성합니다.
- `진행`, `수행`, `관련`, `적절히`처럼 행동을 흐리는 표현을 줄입니다.
- 같은 개념에는 같은 용어를 사용합니다.
- 명령, 경로, 버전, 코드와 API 예시는 현재 프로젝트에서 확인한 값만 작성합니다.

### 문서 검토

- 상대 링크가 실제 파일을 가리키는지 확인합니다.
- 설명한 동작과 계약을 코드, 설정, 테스트와 대조합니다.
- 현재 구현과 향후 계획을 구분합니다.
- Markdown 제목 구조, 코드 블록, 표, 마지막 줄바꿈과 불필요한 공백을 확인합니다.

작성 원칙의 자세한 배경과 예시는
[toss/technical-writing](https://github.com/toss/technical-writing)에서 확인할 수
있습니다.

## 9. 보안과 환경 변수

- 토큰, 비밀번호, OAuth secret, DB 접속정보와 개인 식별정보를 코드와 로그에 남기지
  않습니다.
- 브라우저에 전달되는 `VITE_*` 환경 변수에는 공개 가능한 값만 사용합니다.
- GitHub Actions와 배포 비밀값은 GitHub Secrets 또는 배포 환경에서 주입합니다.
- 인증 토큰을 `localStorage` 또는 `sessionStorage`에 저장하지 않습니다.
