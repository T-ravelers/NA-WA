/**
 * Merchant 도메인 문구 (vi).
 *
 * 아직 일부만 번역했다. 여기 없는 key는 `en`으로 폴백한다.
 * 번역을 늘릴 때 `en.ts`와 같은 구조를 유지한다.
 */
export default {
  merchant: {
    register: {
      irreversible:
        'Thao tác này không thể hoàn tác. Tài khoản cửa hàng không thể thanh toán hoặc sử dụng các tính năng dành cho du khách.',
    },
  },
}
